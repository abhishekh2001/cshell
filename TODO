Host name

ls; cd ..; ls -l; ls -al
Error: corrupted size vs. prev_size
    seems like it works if there are no spaces
    and it works for anything not ending in -l


ls; cd ..; ls -l; ls -al; cd ..; ls -l -a
l
